import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles((theme) => ({
	video: {
		position: 'relative',
		zIndex: 0,
		width: 400,
		height: 300,
	},
}))

export default useStyles
